{
  "name": "petshop-app",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "dev:api": "pnpm --filter @petshop/api start:dev",
    "dev:web": "pnpm --filter @petshop/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
